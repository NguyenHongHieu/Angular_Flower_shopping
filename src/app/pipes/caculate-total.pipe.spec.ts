import { CaculateTotalPipe } from './caculate-total.pipe';

describe('CaculateTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new CaculateTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
